{
  "name": "cf-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev --config wrangler.local.toml",
    "deploy": "wrangler pages deploy . --project-name wuma-edge --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
